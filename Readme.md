An issue in ` com.google.firebase.crashlytics.buildtools.gradle.tasks.GenerateSymbolFileTask` with enable configuration cache.

To reproduce the error run `./gradlew :app:generateCrashlyticsSymbolFileRelease`